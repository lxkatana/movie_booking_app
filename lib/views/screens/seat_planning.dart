import 'package:flutter/material.dart';

class SeatPlanning extends StatelessWidget {
  SeatPlanning({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        body: Column(
          children: [
            
          ],
        ),

        
      ),
    );
  }
}